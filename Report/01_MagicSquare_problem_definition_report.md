# 4×4 마방진 프로그램 — 문제 인식 보고서

> 작성일: 2026-05-28
> 단계: STEP 1 ~ STEP 4 (문제 인식 단계)
> 목적: 구현 전 문제의 본질을 관찰·분석·정의하기

---

## 목차

1. [STEP 1 — Observation (관찰)](#step-1--observation)
2. [STEP 2 — Why 분석: 마방진을 완성해야 하는가?](#step-2--why)
3. [STEP 3 — Why 분석: 왜 프로그램으로 구현하는가?](#step-3--why)
4. [STEP 4 — Why 분석: 왜 TDD 방식으로 설계하는가?](#step-4--tdd)
5. [STEP 5 — 문제 정의 문서](#step-5)

---

## STEP 1 — Observation (관찰)

### 1. 현재 우리가 해결하려는 상황은 무엇인가?

1부터 16까지의 서로 다른 정수를 4x4 격자에 배치할 때,
어떤 배치가 "균형 조건"을 만족하는가를 판별하고 생성하는 상황이다.

구체적으로 관찰되는 상황:

- 16개의 숫자가 존재하며, 이들을 16개의 칸에 중복 없이 채워야 한다
- 배치의 경우의 수는 16! 약 2조 개 이상으로 방대하다
- 그 중 모든 행, 열, 대각선의 합이 동일한 값(34)이 되는 배치만이 유효하다
- 유효한 배치를 찾는 기준이 명확하지만, 탐색 공간이 극단적으로 넓다

즉, 현재 상황은 "제약 조건을 만족하는 배치를 방대한 공간에서 효율적으로 탐색해야 하는 문제"다.

### 2. 왜 4x4 마방진 문제를 다루는가?

| 관찰 항목 | 내용 |
|---|---|
| 크기의 적절성 | 3x3은 너무 단순(해가 1가지), 5x5 이상은 너무 복잡 - 4x4는 복잡성과 가시성의 균형점 |
| 해의 다양성 | 4x4 마방진의 본질적으로 다른 해는 880가지 - 단일 정답이 아니라 탐색과 검증의 대상 |
| 제약의 명확성 | 합의 조건(34)이 수학적으로 명확 - 정답 여부를 즉시 검증할 수 있음 |
| 역사적 맥락 | 뒤러의 "멜랑콜리아"(1514)에 등장 - 수학적 미학과 완결성의 상징 |

### 3. 어떤 학습 또는 시스템 설계 맥락에서 등장하는가?

알고리즘 학습 맥락:
- 완전 탐색(Brute Force)과 그 한계 인식
- 백트래킹(Backtracking)을 통한 가지치기(pruning) 전략
- 제약 충족 문제(CSP, Constraint Satisfaction Problem)의 실습 사례

시스템 설계 맥락:
- 입력 -> 검증 -> 출력의 명확한 파이프라인
- 하나의 해 vs. 모든 해 탐색이라는 목적의 선택이 설계에 영향을 줌
- 시각적 출력(4x4 격자 표시)이 요구되는 출력 설계 문제

문제 정의 맥락:
- "만든다"는 행위 이면에는 탐색, 검증, 선택, 표현이라는 4개의 하위 문제가 숨어 있음

관찰 요약:
4x4 마방진 문제는 "숫자를 채운다"는 단순한 행위처럼 보이지만,
실제로는 방대한 탐색 공간에서 다중 제약 조건을 동시에 만족하는 상태를 효율적으로 찾고 표현하는 문제다.
이 문제의 본질은 배치 자체가 아니라, 탐색 전략과 제약 처리 방식의 선택에 있다.

---

## STEP 2 — Why 분석: 마방진을 완성해야 하는가?

각 가정(A)과 그 가정이 드러내는 구조적 문제를 분석한다.

### A-1. "알고리즘을 배우기 위해서"

| 관찰 | 구조적 문제 |
|---|---|
| 마방진 완성이 목적처럼 보이지만, 실제 목적은 알고리즘 이해 | 목적과 수단이 뒤바뀌어 있다 |
| 알고리즘을 배웠다면, 마방진이 "완성"되어야 하는가? | 완성 기준이 모호하다 - 하나의 해? 모든 해? 가장 빠른 해? |
| 마방진 외에도 같은 알고리즘을 배울 수 있는 문제는 많다 | 왜 하필 마방진인가라는 질문이 해소되지 않는다 |

구조적 문제: 학습 목적이라면 완성이 아니라 탐색 과정 자체가 결과물이어야 한다.

### A-2. "정해진 규칙을 만족하는 결과물을 출력하기 위해서"

| 관찰 | 구조적 문제 |
|---|---|
| 출력 조건(합=34)은 명확하지만, 어떤 배치를 출력할지는 결정되지 않았다 | 880가지 해 중 선택 기준이 없다 |
| "완성"을 출력으로 정의하면, 프로그램은 단순 출력기가 된다 | 탐색 로직과 출력 로직이 분리되지 않은 채 설계될 위험 |

구조적 문제: "완성 = 출력"으로 정의하면 시스템이 정적 생성기에 머문다.

### A-3. "수학적 완결성을 구현으로 증명하기 위해서"

| 관찰 | 구조적 문제 |
|---|---|
| "아름다운 구조"는 주관적 판단이다 | 완성의 기준이 수학인가, 코드인가, 사람의 감상인가 - 불명확 |
| 수학적 증명은 이미 되어 있다 (880가지 해가 존재함은 알려진 사실) | 구현이 새로운 지식을 생산하는가, 아니면 알려진 사실을 재현하는가 |

### A-4. "사용자가 요청했기 때문에"

| 관찰 | 구조적 문제 |
|---|---|
| 요청이 있다고 해서 왜(Why)가 해소되는 것은 아니다 | Why를 외부에 위임하면, 설계자가 판단 기준을 잃는다 |
| "완성"의 범위를 요청자가 정의하지 않았다면 | 암묵적 가정이 설계 전반에 퍼진다 |

### 공통 결론

```
A-1  ->  목적과 수단이 전도되어 있다
A-2  ->  완성의 범위와 선택 기준이 정의되지 않았다
A-3  ->  완성의 기준이 수학, 코드, 감상 중 무엇인지 불명확하다
A-4  ->  Why를 외부에 위임하면 설계 판단 기준이 사라진다
```

"마방진을 완성해야 한다"는 문장은 Why가 없는 What이다.
완성이라는 단어 안에 탐색 전략, 해의 선택, 출력 형식, 사용자 목적이 모두 압축되어 있다.

---

## STEP 3 — Why 분석: 왜 프로그램으로 구현하는가?

### 관점 1 — 반복 가능성 (Repeatability)

16!개의 경우의 수를 사람이 반복하는 것은 물리적으로 불가능하다.
프로그램은 동일한 탐색 로직을 조건이 충족될 때까지 자동으로 반복하며,
반복의 횟수, 속도, 종료 조건을 명시할 수 있다.

핵심: 반복 가능성은 단순 편의가 아니라, 해결 가능성의 조건이다.

### 관점 2 — 검증 자동화 (Verification Automation)

단순 계산에서는 배치 하나당 10번의 수동 계산이 필요하다.
프로그램은 검증 로직을 탐색과 독립적으로 분리하여, 탐색 중 매 단계마다 호출 가능하게 만든다.

핵심: 자동화된 검증은 탐색과 검증을 분리 가능한 독립적 관심사로 만드는 설계 결정이다.

### 관점 3 — 오류 방지 (Error Prevention)

| 오류 유형 | 단순 계산 | 프로그램 |
|---|---|---|
| 숫자 중복 배치 | 검증 후에야 발견 | 구조적으로 불가능하게 설계 |
| 합산 오류 | 즉시 감지 불가 | 자동 계산 |
| 범위 오류 | 의식하지 않으면 놓침 | 입력 단계에서 차단 |

핵심: 프로그램은 오류를 "나중에 발견"하는 것이 아니라 오류가 발생할 수 없는 구조를 설계한다.

### 관점 4 — 규칙 기반 사고 훈련 (Rule-Based Thinking)

| 사고 항목 | 단순 계산 | 프로그램 구현 |
|---|---|---|
| 조건 표현 | 암묵적, 직관적 | 명시적, 형식적 |
| 결정 기준 | 경험 의존 | 로직 의존 |
| 전달 가능성 | 설명하기 어려움 | 코드가 곧 설명 |
| 재현 가능성 | 같은 사람도 매번 다름 | 항상 동일한 결과 |

핵심: 프로그램으로 구현한다는 것은 "내가 아는 것"을 "기계가 실행할 수 있는 언어"로 번역하는 훈련이다.

### 종합

```
반복 가능성    ->  해결 가능성의 조건을 확보한다
검증 자동화    ->  탐색과 검증을 독립적 관심사로 분리한다
오류 방지      ->  오류를 발견하는 것이 아니라 불가능하게 설계한다
규칙 기반 사고 ->  직관을 형식으로 번역하는 과정 자체가 목적이다
```

---

## STEP 4 — Why 분석: 왜 TDD 방식으로 설계하는가?

TDD는 테스트 도구가 아니다.
"무엇이 올바른가를 먼저 정의하고, 그 정의에 맞게 코드를 수렴시키는 것"이다.

### 무엇이 통제되어야 하는가?

| 통제 대상 | 통제되지 않을 때 발생하는 일 |
|---|---|
| 숫자의 범위 (1~16) | 17이 들어가도 합이 34가 될 수 있다 |
| 숫자의 중복 여부 | 같은 숫자가 두 번 나와도 합 조건을 통과할 수 있다 |
| 행/열/대각선의 합 | 일부만 34이고 나머지는 아닌 배치가 "완성"으로 취급될 수 있다 |
| 격자의 완전성 | 빈 칸이 있는 상태로 유효하다고 판정될 수 있다 |

통제의 계층 구조:

```
Level 0: 입력 통제   ->  유효하지 않은 숫자가 격자에 들어올 수 없다
Level 1: 배치 통제   ->  중복 없이 1~16이 정확히 한 번씩 사용되었다
Level 2: 조건 통제   ->  행, 열, 대각선의 합이 모두 34다
Level 3: 결과 통제   ->  출력이 4x4 형태로 올바르게 표현된다
```

### 어떤 불변 조건이 존재하는가?

수학적 불변 조건:

```
INV-1: 1부터 16까지의 정수가 정확히 한 번씩 사용된다
INV-2: 모든 행의 합 = 34
INV-3: 모든 열의 합 = 34
INV-4: 주대각선의 합 = 34
INV-5: 반대각선의 합 = 34
```

구조적 불변 조건:

```
INV-6: 격자는 정확히 4행 4열이다
INV-7: 모든 칸이 채워져 있다 (None, 0, 공백 없음)
INV-8: 합의 기준값은 n(n²+1)/2 = 34로 고정된다 (n=4일 때)
```

핵심: 불변 조건을 먼저 열거하는 행위 자체가 TDD의 첫 번째 단계다.

### 왜 입력/출력이 명확해야 하는가?

```
명확한 입출력
  ->  테스트에서 "주어진 입력에 대해 기대하는 출력"을 쓸 수 있다
  ->  테스트가 실행 가능해진다
  ->  코드가 테스트를 통과하도록 수렴한다
  ->  인터페이스가 구현보다 먼저 확정된다

불명확한 입출력
  ->  테스트를 쓸 수 없다
  ->  코드가 먼저 작성된다
  ->  인터페이스가 구현에 종속된다
  ->  나중에 변경하면 모든 것이 흔들린다
```

핵심: 입출력의 명확성은 TDD의 전제 조건이 아니라, TDD가 만들어내는 결과물이다.

---

## STEP 5 — 문제 정의 문서

### 표면 문제 정의 (잘못된 정의)

"1부터 16까지의 숫자를 4x4 격자에 배치하여,
모든 행·열·대각선의 합이 34가 되도록 마방진을 완성하는 프로그램을 만든다."

왜 이 정의가 잘못되었는가:

| 문제 항목 | 내용 |
|---|---|
| 목적의 부재 | "완성"이 무엇인지 정의되지 않았다 |
| 수신자의 부재 | 누구를 위한 프로그램인가가 명시되지 않았다 |
| 완성 기준의 모호함 | 하나의 해인가, 모든 해인가, 특정 해인가 |
| What만 있고 Why가 없다 | 무엇을 만드는지는 서술하지만, 왜 만드는지는 없다 |
| 수단이 목적으로 둔갑 | 마방진 생성이 목적처럼 보이지만 실제 목적이 아닐 수 있다 |
| 암묵적 가정의 내포 | "배치한다", "완성한다"는 표현 안에 설계 결정이 숨어 있다 |

---

### 개선된 문제 정의 (정확한 정의)

상황:
16개의 서로 다른 정수(1~16)를 4x4 격자에 배치할 때,
수학적으로 균형 잡힌 배치가 존재하는가를 판별하고 표현하는 문제다.

목적:
- 방대한 탐색 공간에서 복수의 불변 조건을 동시에 만족하는 상태를 찾는다
- 찾은 상태를 명확한 형식으로 표현하고 검증한다
- 이 과정을 재현 가능하고 신뢰할 수 있는 방식으로 설계한다

완성의 정의:

| 구분 | 내용 |
|---|---|
| 최소 완성 | 불변 조건을 모두 만족하는 배치가 1개 이상 존재함을 확인하고 표현한다 |
| 확장 완성 | 조건을 만족하는 모든 배치를 열거하고 그 수를 확인한다 |
| 검증 완성 | 임의의 배치가 조건을 만족하는지 판별할 수 있다 |

세 가지 완성은 독립적으로 정의 가능하며, 각각 별도의 검증 대상이다.

---

### 이 문제의 핵심 Invariant

수학적 Invariant:

| ID | 조건 | 설명 |
|---|---|---|
| INV-M1 | 사용된 정수의 집합 = {1, 2, 3, ..., 16} | 범위를 벗어나거나 빠진 수가 없다 |
| INV-M2 | 모든 정수는 정확히 한 번만 사용된다 | 중복 없음, 누락 없음 |
| INV-M3 | 4개 행 각각의 합 = 34 | 행 조건 |
| INV-M4 | 4개 열 각각의 합 = 34 | 열 조건 |
| INV-M5 | 주대각선의 합 = 34 | 좌상 -> 우하 |
| INV-M6 | 반대각선의 합 = 34 | 우상 -> 좌하 |

구조적 Invariant:

| ID | 조건 | 설명 |
|---|---|---|
| INV-S1 | 격자는 정확히 4행 4열이다 | 형태 불변 |
| INV-S2 | 모든 칸이 채워져 있다 | 공백, 미정의 값 없음 |
| INV-S3 | 기준 합 = n(n²+1)/2, n=4일 때 34 | 합의 유일성 보장 |

Invariant 간의 관계:

```
INV-M1 + INV-M2  ->  배치의 유효성 (누가 들어왔는가)
INV-M3 ~ INV-M6  ->  균형의 유효성 (어떻게 배치되었는가)
INV-S1 + INV-S2  ->  형태의 유효성 (구조가 온전한가)

세 종류가 모두 충족될 때만 "유효한 마방진"이다.
하나라도 위반되면 나머지 조건의 충족 여부와 관계없이 무효다.
```

---

### 우리가 실제로 훈련하려는 사고 능력

1. 조건의 형식화 능력
   직관적으로 "맞다"고 느끼는 것을 명제로 표현할 수 있는가.
   "균형 잡혀 있다"는 감각을 수치 조건으로 번역하고,
   모호한 언어를 참/거짓으로 판별 가능한 문장으로 바꾼다.

2. 문제 분해 능력
   하나의 큰 문제를 독립적으로 검증 가능한 단위로 쪼개는 능력.
   "마방진을 만든다"는 하나의 행위가 아니라
   탐색 / 검증 / 선택 / 표현 의 결합이다.

3. 불변 조건 우선 사고
   구현을 생각하기 전에 "무엇이 항상 참이어야 하는가"를 먼저 묻는 습관.
   불변 조건은 설계의 뼈대다.
   뼈대가 없으면 구현이 먼저 완성되고 조건은 나중에 끼워 맞춰진다.

4. Why -> What -> How 순서 사고

   ```
   Why  : 제약 조건을 만족하는 상태를 재현 가능하게 찾고 검증한다
   What : 생성기, 검증기, 표현기의 세 역할
   How  : (이 단계는 아직이다)
   ```

   이 순서가 역전되면 - How가 Why를 결정하게 되면 -
   구현이 목적을 지배하고, 목적은 구현에 종속된다.

5. 경계 인식 능력

   | 경계 항목 | 이 문제의 경계 |
   |---|---|
   | 입력 경계 | 1~16, 중복 없음, 4x4 |
   | 출력 경계 | 유효한 배치의 표현, 또는 유효/무효 판정 |
   | 책임 경계 | "어떤 배치가 유효한가"를 다루며, "어떤 배치가 더 좋은가"는 다루지 않는다 |
   | 확장 경계 | n×n으로의 일반화는 이 문제의 범위 밖이다 |

---

## 최종 요약

```
표면 정의      ->  What만 있고 Why가 없으며 완성 기준이 모호하다
개선된 정의    ->  탐색/검증/표현의 분리와 세 가지 완성 기준을 명시한다
핵심 Invariant ->  수학적(M1~M6) + 구조적(S1~S3), 모두 충족 시에만 유효하다
훈련 목표      ->  형식화 / 분해 / 불변 조건 우선 / Why->What->How / 경계 인식
```

마방진을 만드는 것이 목적이 아니다.
마방진이라는 재료를 통해,
조건을 먼저 정의하고 구현을 나중에 결정하는 사고 방식을 훈련하는 것이 목적이다.
